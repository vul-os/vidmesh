//! Vidmesh relay binary.
//!
//! Phase 4 implements the relay per `spec/006-relay.md`: `WS /sync` with
//! filter subscriptions, an optional verified blob sidecar, `GET /info`
//! policy document, PoW/rate-limit anti-spam, and gossip between peers.

#![forbid(unsafe_code)]

fn main() {
    eprintln!("vidmesh-relay: Phase 4 not implemented yet (Phase 0 scaffold)");
    std::process::exit(1);
}

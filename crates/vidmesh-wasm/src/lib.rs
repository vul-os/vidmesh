//! wasm-bindgen wrapper over `vidmesh-kernel`.
//!
//! Phase 3 exposes record creation/verification, id derivation, blob
//! hashing, and identity chain helpers to JS via `packages/kernel-ts`.

#![forbid(unsafe_code)]

//! Vidmesh node app.
//!
//! Phase 8 turns this into a Tauri 2 scaffold: a background app that pins
//! and seeds chosen content (own videos first, subscriptions second) with no
//! public-facing duties.

#![forbid(unsafe_code)]

fn main() {
    eprintln!("vidmesh-node: Phase 8 not implemented yet (Phase 0 scaffold)");
    std::process::exit(1);
}

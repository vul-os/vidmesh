# Vidmesh specification

The normative Vidmesh protocol specification, one concern per numbered file
(`000-overview.md` through `011-threat-model.md`), licensed CC-BY-SA-4.0
(see `../LICENSE-SPEC`).

**Status: not yet written.** Phase 1 transcribes and expands
`VIDMESH_SPEC_PROPOSAL.md` — the authoritative source for all protocol
semantics — into this folder. That document is not present in the repository
yet; the spec cannot be written without it (the build plan forbids inventing
protocol semantics).

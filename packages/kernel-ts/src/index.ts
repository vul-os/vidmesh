/**
 * @vidmesh/kernel — typed API over the Vidmesh WASM kernel.
 *
 * Phase 3 implements: createRecord, verifyRecord, deriveId, hashBlobStream
 * (WHATWG streams), and Identity chain helpers, all backed by the
 * vidmesh-wasm build so behavior is byte-identical to the Rust kernel.
 *
 * Phase 0 scaffold — no implementation yet.
 */

export const PHASE = 0;

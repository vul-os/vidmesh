/**
 * Vidmesh gateway web frontend.
 *
 * Phase 6 implements: home, watch page (player + client-side verification
 * badge + threaded comments), channel page, upload flow, and the moderation
 * policy page. React 18 + Vite + Tailwind + TanStack Query.
 *
 * Phase 0 scaffold — no implementation yet.
 */

export const PHASE = 0;

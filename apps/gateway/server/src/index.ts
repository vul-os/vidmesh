/**
 * Vidmesh gateway server.
 *
 * Phase 5 implements: relay ingest with a selection policy engine, SQLite
 * index, content-addressed blob store, upload/transcode pipeline, HLS
 * serving, REST API for the frontend, and the compliance toolkit.
 *
 * Phase 0 scaffold — no implementation yet.
 */

console.error("gateway-server: Phase 5 not implemented yet (Phase 0 scaffold)");
process.exit(1);
